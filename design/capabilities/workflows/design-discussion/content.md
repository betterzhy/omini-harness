# Design Discussion Workflow

This workflow coordinates discussion stages; it is not a workflow execution engine. Stages may be skipped only where allowed by the declared transition map and explicit project state.
