# Java Project Profile 模板

复制 `java-project-profile.yaml` 到目标项目后，由项目 Profile Owner 选择 Java Profile、Baseline、模块边界和质量配置路径。

该文件只表达 Java 能力输入选择，不是项目采用 Authority，也不保存审批、候选运行结果、来源锁、任务完成、合入或发布状态。项目业务 Authority 与该 Profile 冲突时，由目标项目记录并裁决冲突。
