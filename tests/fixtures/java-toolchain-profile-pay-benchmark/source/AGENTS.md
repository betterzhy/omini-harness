# Test-only guidance

This synthetic source exists only to measure Harness validation reuse. Treat
all operations as read-only and all business execution permissions as denied.
Its facts are canonical only inside this synthetic fixture. It is not Pay-Nexus
or production project Authority and must never be registered as adoption evidence.
