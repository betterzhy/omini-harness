# Tooling Boundary

`tooling/` is reserved for repository-local implementation tooling that does not itself carry canonical design meaning.

Canonical design capability lives under `design/capabilities/`. Shared schema/governance mechanics live under `core/`. Runtime packaging lives under `runtime/` and `generated/`. Do not use this directory as a prompt library, note bucket, or alternative knowledge source.
